@import 'lib';

#chart-config {
  padding: 0 $med-padding;

  label {
    display: block;
    margin: 10px;
    padding: 5px 0;
    border: 1px solid transparent;

    &:hover {
      border: 1px solid $mild-blue;
    }
  }

  button {
    display: block;
    width: 100%;
    line-height: 20px;
  }
}
