@import 'lib';

.button {
  background-color: #00224a;
  border-radius: 3px;
  color: #fff;
  cursor: pointer;
  font-size: 12px;
  font-weight: 200;
  padding: 10px;

  &:hover {
    background-color: $mild-blue;
  }
}
